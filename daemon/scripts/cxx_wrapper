#!/bin/bash
# Workaround: tectonic_xetex_layout hardcodes -std=c++14, but ICU 75+ headers
# require C++17. Replace the flag before passing to the real compiler.
args=()
for arg in "$@"; do
    [ "$arg" = "-std=c++14" ] && args+=("-std=c++17") || args+=("$arg")
done
exec /usr/bin/c++ "${args[@]}"
